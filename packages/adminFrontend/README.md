# Restaurant Admin Frontend

This is a self-contained React + Vite frontend project for a restaurant admin dashboard.
All data is stored in React state (mock data) and supports full CRUD in the UI.

## Setup

1. Install dependencies:
```bash
npm install
```

2. Run dev server:
```bash
npm run dev
```

Open the dev URL (usually http://localhost:5173).

